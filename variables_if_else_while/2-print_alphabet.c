#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char julieth;

	for (julieth = 'a'; julieth <= 'z'; julieth++)
		putchar(julieth);
	putchar('\n');
	return (0);
}
